<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

       <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.mariadb.jdbc.Driver</property>
        <property name="connection.url">jdbc:mariadb://192.168.178.28:3306/dunaevi</property>
        <property name="connection.username">evgeny</property>
        <property name="connection.password">08qfZnKcdYP4JTRw</property>

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MariaDBDialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>

		<!-- Set the current session context -->
		<property name="current_session_context_class">thread</property>
 
    </session-factory>

</hibernate-configuration>
<!-- <!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        JDBC Database connection settings
        <property name="connection.driver_class">org.mariadb.jdbc.Driver</property>
        <property name="connection.url">jdbc:mariadb://localhost:3306/dev_first</property>
        <property name="connection.username">local_usr</property>
        <property name="connection.password">1qay2wsx</property>

        JDBC connection pool settings ... using built-in test pool
        <property name="connection.pool_size">1</property>

        Select our SQL dialect
        <property name="dialect">org.hibernate.dialect.MariaDBDialect</property>

        Echo the SQL to stdout
        <property name="show_sql">true</property>

		Set the current session context
		<property name="current_session_context_class">thread</property>
 
    </session-factory>

</hibernate-configuration> -->